# EX-Digital Frontend — Docker Production Environment
# Used when building the Docker image (Dockerfile runs npm run build).
# nginx proxies /auth/ /courses/ /sessions/ /attendance/ /admin/ /health
# to backend:8000 internally, so no absolute URL is needed.

VITE_API_URL=
